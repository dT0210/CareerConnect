import { useNavigate } from "react-router-dom";

const DashBoard = () => {
    const navigate = useNavigate();
    return (
        <div className="p-4">
            Dash Board
            
        </div>
    );
};

export default DashBoard;